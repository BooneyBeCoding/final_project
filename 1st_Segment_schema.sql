-- Creating table for final_projectDB
CREATE TABLE reddit (
	title VARCHAR (40) NOT NULL,
	score INT,
	subreddit VARCHAR,
	url INT,
	num_comments VARCHAR,
	body VARCHAR,
	date VARCHAR
);

SELECT * FROM reddit;
